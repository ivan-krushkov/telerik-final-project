kind: ConfigMap 
apiVersion: v1 
metadata:
  name: easyclaim-frontend
  labels:
    name: easyclaim-frontend
data:
  config: |-
    {
      "authConfig": {
        "backendUrl": "https://api.example.com"
      }
    }

